<toggl:TogglWindow x:Class="TogglDesktop.PomodoroNotification"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:toggl="clr-namespace:TogglDesktop"
    mc:Ignorable="d" Width="280" Height="200"
    Title="Pomodoro Timer" IsToolWindow="True"
    Topmost="True"
    >
    <Grid Background="{StaticResource ViewBackgroundLight}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <toggl:TogglChromeDesignTimeConverter Title="Pomodoro Timer" IsToolWindow="True"/>

        <StackPanel Grid.Row="0" Margin="10">
            <TextBlock Text="You have been working for 25 minutes."
                       Name="timeText" x:FieldModifier="private"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="8" HorizontalAlignment="Right" Orientation="Horizontal">
            <Button Content="STOP" Style="{StaticResource FlatButton}"
                    Click="onStopClicked"/>
            <Button Content="RESET TIMER" Style="{StaticResource FlatButton}"
                    Click="onResetClick"/>
        </StackPanel>
        
    </Grid>
</toggl:TogglWindow>
