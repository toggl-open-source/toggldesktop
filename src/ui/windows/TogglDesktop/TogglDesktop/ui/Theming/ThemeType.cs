namespace TogglDesktop.Theming
{
    internal enum ThemeType
    {
        Layout = 1,
        ColorScheme = 2,
    }

    public enum ColorScheme
    {
        Light = 1,
        Dark = 2,
    }
}